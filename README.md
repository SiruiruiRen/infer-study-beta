# INFER Study Beta - Treatment Group 2

## Study Condition: INFER Only (No Tutorial Video)

This is the **Treatment Group 2** version of the INFER 4-video experiment.

### Video Configuration

| Video | INFER Feedback | Description |
|-------|----------------|-------------|
| Video 1 | ❌ No | Reflection only (baseline) |
| Video 2 | ✅ Yes | Full INFER with PV analysis |
| Video 3 | ✅ Yes | Full INFER with PV analysis |
| Video 4 | ❌ No | Reflection only (post-test) |

### Study Flow

```
Welcome/Consent → Login → Pre-Survey (MANDATORY)
    ↓
Video 1: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Video 2: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 3: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 4: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Post-Survey (MANDATORY) → Thank You
```

### Configuration Required

1. Update `SUPABASE_URL` and `SUPABASE_KEY` in `app.js`
2. Update `VIDEOS` array with actual video links
3. Update `QUALTRICS_SURVEYS` with survey links

### Deployment

Deploy to Render.com as a static site.

URL Pattern: `infer-study-beta.onrender.com`


## Study Condition: INFER Only (No Tutorial Video)

This is the **Treatment Group 2** version of the INFER 4-video experiment.

### Video Configuration

| Video | INFER Feedback | Description |
|-------|----------------|-------------|
| Video 1 | ❌ No | Reflection only (baseline) |
| Video 2 | ✅ Yes | Full INFER with PV analysis |
| Video 3 | ✅ Yes | Full INFER with PV analysis |
| Video 4 | ❌ No | Reflection only (post-test) |

### Study Flow

```
Welcome/Consent → Login → Pre-Survey (MANDATORY)
    ↓
Video 1: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Video 2: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 3: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 4: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Post-Survey (MANDATORY) → Thank You
```

### Configuration Required

1. Update `SUPABASE_URL` and `SUPABASE_KEY` in `app.js`
2. Update `VIDEOS` array with actual video links
3. Update `QUALTRICS_SURVEYS` with survey links

### Deployment

Deploy to Render.com as a static site.

URL Pattern: `infer-study-beta.onrender.com`


## Study Condition: INFER Only (No Tutorial Video)

This is the **Treatment Group 2** version of the INFER 4-video experiment.

### Video Configuration

| Video | INFER Feedback | Description |
|-------|----------------|-------------|
| Video 1 | ❌ No | Reflection only (baseline) |
| Video 2 | ✅ Yes | Full INFER with PV analysis |
| Video 3 | ✅ Yes | Full INFER with PV analysis |
| Video 4 | ❌ No | Reflection only (post-test) |

### Study Flow

```
Welcome/Consent → Login → Pre-Survey (MANDATORY)
    ↓
Video 1: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Video 2: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 3: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 4: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Post-Survey (MANDATORY) → Thank You
```

### Configuration Required

1. Update `SUPABASE_URL` and `SUPABASE_KEY` in `app.js`
2. Update `VIDEOS` array with actual video links
3. Update `QUALTRICS_SURVEYS` with survey links

### Deployment

Deploy to Render.com as a static site.

URL Pattern: `infer-study-beta.onrender.com`


## Study Condition: INFER Only (No Tutorial Video)

This is the **Treatment Group 2** version of the INFER 4-video experiment.

### Video Configuration

| Video | INFER Feedback | Description |
|-------|----------------|-------------|
| Video 1 | ❌ No | Reflection only (baseline) |
| Video 2 | ✅ Yes | Full INFER with PV analysis |
| Video 3 | ✅ Yes | Full INFER with PV analysis |
| Video 4 | ❌ No | Reflection only (post-test) |

### Study Flow

```
Welcome/Consent → Login → Pre-Survey (MANDATORY)
    ↓
Video 1: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Video 2: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 3: Watch → Reflect → INFER Feedback → Revise → Submit → Survey (MANDATORY)
    ↓
Video 4: Watch → Reflect → Submit → Survey (MANDATORY)
    ↓
Post-Survey (MANDATORY) → Thank You
```

### Configuration Required

1. Update `SUPABASE_URL` and `SUPABASE_KEY` in `app.js`
2. Update `VIDEOS` array with actual video links
3. Update `QUALTRICS_SURVEYS` with survey links

### Deployment

Deploy to Render.com as a static site.

URL Pattern: `infer-study-beta.onrender.com`

